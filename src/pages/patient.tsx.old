import { Card, CardSection, Frame } from "app/components";
import { Clippy, ArrowUpRight, Clock } from "app/icons";

import classes from "./patient.module.scss";

export function Action() {
    return (
        <Card as="button" className={classes.card}>
            <CardSection>
                <div className={classes.header}>
                    <div className={classes.box}>
                        <Clippy height="24" />
                    </div>
                    <ArrowUpRight className={classes.arrow} height="24" />
                </div>
                <div className={classes.body}>
                    <h1 className={classes.title}>Agitated Behaviour Scale</h1>
                    <p className={classes.description}>
                        The Agitated Behaviour Scale (ABS) measures behavioral
                        aspects of agitation during the acute phase of recovery
                        from acquired brain injury including aspects of
                        aggression, disinhibition, and lability.
                    </p>
                </div>
            </CardSection>
            <CardSection variant="secondary">
                <div className={classes.footer}>
                    <Clock height="24" />
                    <div className="">
                        <p>Last Administered</p>
                        <p>11th December '20</p>
                    </div>
                </div>
            </CardSection>
        </Card>
    );
}

export default function Patient() {
    return (
        <Frame title="Patient">
            <Action />
            <Action />
            <Action />
        </Frame>
    );
}
